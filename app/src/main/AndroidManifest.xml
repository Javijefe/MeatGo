<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeatGo"
        tools:targetApi="31"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity" android:exported="true" />
        <activity android:name=".ProductosActivity" android:exported="true" />
        <activity android:name=".CarritoActivity" android:exported="true" />
        <activity android:name=".MisPedidosActivity" android:exported="true"/>
        <activity android:name=".MisPedidosAcabadosActivity" android:exported="true"/>
        <activity android:name=".AdminLoginActivity" android:exported="true"/>
        <activity android:name=".AdminProductosActivity" android:exported="true"/>
        <activity android:name=".AdminPedidosActivity" android:exported="true"/>
        <activity android:name=".AdminDetallesPedidos" android:exported="true"/>

    </application>

</manifest>